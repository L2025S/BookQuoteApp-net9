
## Environtment variables

JWT_SECRET= YOUR_JWT_SECRET
ConnectionStrings_BookQuote=YOUR_NEON_DATABASE_CONNECTION_STRING

## To connect it to the frontend project, add the deployed frontend URL to Program.cs. 
## If you are testing locally, this backend project listens on localhost:8080 due to the CORS configuration.
